{
  "name": "network-monitor-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "rm -rf out && next build && rm -rf ../../docs && mv out ../../docs && (touch ../../docs/.nojekyll || true)",
    "build-docs": "npm run build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "next": "^14.1.0",
    "plotly.js": "^2.27.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-plotly.js": "^2.6.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "postcss": "^8.4.31"
  }
}
